# Build-time dependencies for the controlled Polish source audit.
# The commit pin is intentional: PyPI currently exposes an older 3.1.1 package, while the
# reviewed upstream repository contains the 3.2 data snapshot used by this project.
git+https://github.com/rspeer/wordfreq@912caf64b657478d1dff1138efdc078947d54bb1

# Morfeusz 2 / SGJP oracle for first-name homonym auditing and selected first-name inflection generation.
morfeusz2==1.99.15

# Pinned HTTP client used only by the official GUS TERYT retrieval script.
requests==2.34.2

# SGJP data-only fallback for explicit selected first-name and city inflection.
polish-inflection==0.7.3

# PDF text extraction for the official KSNG/GUGiK country-capital source.
# PDF text extraction for the official KSNG/GUGiK country-capital source.
pypdf==6.19.0
# fontTools enables pypdf to decode the CFF Type1 fonts used by the KSNG PDF.
fonttools==4.66.0

# PyMuPDF is an independent PDF text-extraction path for the rotated/font-encoded KSNG PDF.
pymupdf==1.28.2
